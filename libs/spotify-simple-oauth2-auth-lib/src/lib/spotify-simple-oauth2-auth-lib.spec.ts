import { spotifySimpleOauth2AuthLib } from './spotify-simple-oauth2-auth-lib'

describe('spotifySimpleOauth2AuthLib', () => {
  it('should work', () => {
    expect(spotifySimpleOauth2AuthLib()).toEqual(
      'spotify-simple-oauth2-auth-lib'
    )
  })
})

import { spotifyExpressPassportAuthLib } from './spotify-express-passport-auth-lib'

describe('spotifyExpressPassportAuthLib', () => {
  it('should work', () => {
    expect(spotifyExpressPassportAuthLib()).toEqual(
      'spotify-express-passport-auth-lib'
    )
  })
})
